version: "3"

services:
  app:
    build: .
    ports:
      - "3002:3000"

  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
x  grafana:
    image: grafana/grafana
    ports:
      - "3001:3000"